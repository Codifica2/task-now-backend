describe("#task", function () {
  it("should accept only the predetermined fields");
  it("should return id in string format");
  it("should allow many userIds in assigned array");
});

describe("#category", function () {
  it("should accept only the predetermined fields");
  it("should return id in string format");
});

describe("#user", function () {
  it("should accept only the predetermined fields");
  it("should return id in string format");
});
